{
  "name": "feedreader",
  "type": "module",
  "scripts": {
    "start": "node server.ts",
    "unread": "node scripts/unread.ts",
    "test": "tsc --noEmit && vitest run"
  },
  "dependencies": {
    "fast-xml-parser": "^5"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2",
    "vitest": "^3"
  }
}
