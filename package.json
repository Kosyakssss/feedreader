{
  "name": "feedreader",
  "type": "module",
  "scripts": {
    "start": "bun server.ts",
    "unread": "bun scripts/unread.ts",
    "test": "tsc --noEmit && bun test --isolate"
  },
  "dependencies": {
    "fast-xml-parser": "^5"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^7.0.2"
  }
}
